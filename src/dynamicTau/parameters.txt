# Gain profile parameters
maxGain=60.0
gammaGain=1

# Axion generation parameters (freq in MHz)
gGammaSim=0.1
axionFreq=6999.5
axionBool=0

# Scanning Parameters (center and step in MHz, resolution in Hz)
sigmaProc=0.1
nSpectra=10
centerFreq=6999
tuningStepFreq=0.1
scanningWindowSize=5


# If changing this freq res or window size, also need to change the input layer size in bayes.hpp
freqWindowSize=5000000
freqRes=100

# Reward Parameters
targetCoupling=0.04

#Tanh Reward
peakReward=60
peakSharpness=1

# Piecewise Reward
belowThresholdValue=-1000
aboveThresholdValue=60
stepPunish=-5
